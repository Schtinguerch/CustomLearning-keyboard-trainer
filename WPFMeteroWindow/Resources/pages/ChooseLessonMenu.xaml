<UserControl x:Class="WPFMeteroWindow.Resources.pages.ChooseLessonMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFMeteroWindow.Resources.pages"
             xmlns:prop="clr-namespace:WPFMeteroWindow.Properties"
             xmlns:res="clr-namespace:WPFMeteroWindow.Resources.localizations"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400" IsVisibleChanged="UserControl_IsVisibleChanged" >
    <Grid 
        Width="300"
        Height="500"
        
        Margin="-30 -8 -30 -8"
        Background="{Binding Path=MainBackground ,Source={x:Static prop:Settings.Default}}">
        
        <ScrollViewer 
            VerticalScrollBarVisibility="Visible"
            HorizontalScrollBarVisibility="Hidden"
            
            VirtualizingStackPanel.IsVirtualizing="True"
            VirtualizingStackPanel.VirtualizationMode="Recycling">

            <StackPanel
                Margin="0 10 0 10"
                x:Name="LessonStackPanel"
                Orientation="Vertical">
                
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
